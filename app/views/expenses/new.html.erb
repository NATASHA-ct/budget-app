<div class="navbar">
  <h1 class="main-title"> New Transactions</h1>
  <% if user_signed_in? %>
     <%= link_to("Log out", destroy_user_session_path, method: :delete) %>    
  <% end %>
</div>

<div class="frm-categ">
    <%= render "form", expense: @expense %>

    <br>

    <div class="to-group">
      <%= link_to "Back to Category Transactions", group_path(@group) %>
    </div>
</div>